/** @type {import('next').NextConfig} */
const nextConfig = {
  // Hide the bottom-left Next.js dev indicator (route / issues badge) in development
  devIndicators: false,
};

export default nextConfig;
